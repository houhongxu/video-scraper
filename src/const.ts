import path from 'path'

export const DOWNLOAD_PATH = path.resolve('./download')
export const LOG_PATH = path.resolve('./log')
export const IMG_PATH = path.resolve('./img')

export const DEFAULT_CONCURRENCY = 9
